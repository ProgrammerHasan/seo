# Twitter/X Cards

```php
Seo::twitter('card', 'summary_large_image')
    ->twitter('site', '@yourhandle')
    ->twitter('creator', '@programmerhasan');
```

Default card is `summary_large_image` when an image exists, otherwise `summary`.
