# Testing and Quality

Install dependencies:

```bash
composer install
```

Run tests:

```bash
composer test
```

Run code style check:

```bash
composer format:test
```

Fix code style:

```bash
composer format
```

Run static analysis:

```bash
composer analyse
```

Run everything:

```bash
composer ci
```
